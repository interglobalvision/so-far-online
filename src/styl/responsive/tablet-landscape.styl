@media screen and (min-width: 1024px)

  $gutter = 40px

  $margin-micro = ($gutter / 4)
  $margin-tiny = ($gutter / 2)
  $margin-small = ($gutter)
  $margin-basic = ($gutter * 2)
  $margin-mid = ($gutter * 3.5)
  $margin-large = ($gutter * 5)

  $font-size-extra = 36px
  $font-size-large = 28px
  $font-size-mid = 17px
  $font-size-basic = 14px
  $font-size-small = 11px
  $font-size-tiny = 10px

  $hover-transition = 0.1s

  grid-maker(100%, $gutter, l)

  space-maker($margin-micro, $margin-tiny, $margin-small, $margin-basic, $margin-mid, $margin-large)

  font-sizes($font-size-basic, $font-size-extra, $font-size-large, $font-size-mid, $font-size-small, $font-size-tiny)

  $header-height = 73px
  $footer-height = 256px
  $shop-menu-height = 59px

  .not-desktop
    display: none !important

  .desktop-only
    display: block !important

  figcaption
    font-size: $font-size-small

  #fixed-search-holder
    top: $header-height

  #main-container
    padding-top: $header-height

  .no-gutter-left
    padding-left: 0 !important

  .no-gutter-right
    padding-right: 0 !important

  #header
    &.nav-up
      top: -($header-height)

  #logo
    height: 32px

  .header-icon
    height: 24px

  #footer-nav
    li
      margin-bottom: $margin-tiny

  #popup-cookies 
    .grid-row 
      flex-wrap: nowrap

  /*#shop-menu
    height: auto !important*/

  /*.sub-menu
    position: absolute
    width: 100%
    height: auto
    top: 100%
    border: 1px solid $color-grey-dark*/

  /*#shop-sub-menu-holder
    position: absolute
    top: 100%
    left: 0
    width: 100%

  .shop-sub-menu
    width: 50%
    margin-left: 50%
    background-color $color-pale
    border: 1px solid $color-grey-dark*/

  #shop-menu
    #shop-menu-holder
      opacity: 1 !important
      transform: translateY(0%) !important
      pointer-events: auto !important

  #shop-menu + article,
  #shop-menu + section
    padding-top: $shop-menu-height + $margin-small

  #shop-menu-holder
    overflow: unset

  #shop-menu-list
    position: relative

  .shop-sub-menu-trigger
    cursor: pointer
    &.grid-item
      padding-left: $margin-tiny
      padding-right: $margin-tiny
    &.show
      .shop-menu-label
        border-bottom: 2px solid $color-black
      > span
        text-decoration: underline
      .shop-sub-menu
        pointer-events: auto
        opacity: 1

  .shop-menu-label
    margin-left: 0
    margin-right: 0
    border-bottom: 0
    font-size: $font-size-basic
    font-weight: normal

  #shop-menu-list li:first-of-type
    .shop-menu-label
      margin-bottom: 0

  .shop-sub-menu
    position: fixed
    width: 100%
    left: 0
    top: 100%
    height: auto
    max-height: calc('100vh - ' + ($shop-menu-height + $header-height - 2))
    background-color: $color-white
    border: none
    border-top: 1px solid $color-grey-dark
    border-bottom: 1px solid $color-grey-dark
    pointer-events: none
    opacity: 0
    overflow: scroll
    padding-top: $margin-small
    cursor: default
    transition: opacity .2s ease-in-out

  .shop-sub-menu-item
    &.grid-item
      padding-left: $margin-tiny
      padding-right: $margin-tiny

  .shop-filter-image-holder
    height: 300px

  .shop-filter-image
    &.show
      display: inline-block

  #main-content
    min-height: calc('100vh - ' + ($header-height * 2))

  .row-l-reverse
    flex-direction: row-reverse

  .swiper-container
    &[data-swiper-type='scroll']
      width: calc('83.3333333333% - ' + $margin-small)
      .swiper-slide
        width: 400px
        &:not(:first-child)
          padding-left: $margin-tiny
        &:not(:last-child)
          padding-right: $margin-tiny

  .slide-prev-holder
    padding-right: $margin-micro

  .slide-next-holder
    padding-left: $margin-micro

  .slide-prev, .slide-next, .overlay-prev, .overlay-next
    &:focus
      outline: none

  .slide-pagination-icon
    height: 8.5px

  a, button
    &.button
      transition: background-color $hover-transition ease-in-out
      &:hover
        background-color: $color-grey-dark

  #article-content
    font-size: $font-size-basic + 2px
    h2, h3, h4, h5, h6
      font-size: $font-size-mid
    h2, h3, h4, h5, h6, ol, ul, figcaption, p, ul, ol, .wp-block-embed
      width: 50%
      margin-left: 25%
      ul, ol
        margin-left: 1rem
        max-width: auto
    /*p
      &:first-of-type
        font-size: $font-size-mid*/
    .wp-block-quote
      p
        width: 40%
        margin-left: 30%
        font-size: $font-size-basic + 2px
      cite
        width: 30%
        margin-left: 35%
        font-size: $font-size-small
    .wp-block-pullquote
      p
        font-size: $font-size-large

  .thumb-holder
    &.product-item-thumb-holder
      height: 460px
    &.scroll-slider-thumb-holder
      height: 280px
    &.further-reading-thumb-holder
      height: 14vw

  .form-grid-item
    padding-left: ($margin-micro / 2)
    padding-right: ($margin-micro / 2)

  #popup-notice
    width: 60vw

  .partner-logo
    max-width: 180px
